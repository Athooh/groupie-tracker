<!-- Albums & Members -->
    <!-- <section class="albums-members"> -->
        <!-- <div class="albums">
            <h2>Albums</h2>
            <div class="album-grid">
                <!-- {{range .Albums}} -->
                <!-- <div class="album-card">
                    <img src="album-cover.jpg" alt="Album Cover">
                    <p>{{.}}</p>
                </div>
                {{end}}
            </div>
        </div> --> 
        <!-- <div class="members">
            <h2>Members</h2>
            <ul>
                {{Join .Artist.Members ", "}}
                <li>{{.}}</li>
                {{end}}
            </ul>
        </div> -->
    <!-- </section> -->

    <!-- Tour Dates & Locations -->
    <!-- <section class="tour-dates-locations">
        <div class="tour-dates">
            <h2>Tour Dates</h2>
            <ul>
                {{range .Dates}}
                <li>{{.}}</li>
                {{end}}
            </ul>
        </div>
        <div class="locations">
            <h2>Locations</h2>
            <ul>
                {{range .Locations}}
                <li>{{.}}</li>
                {{end}}
            </ul>
        </div>
    </section> -->

    <!-- Relations -->
    <!-- <section class="relations">
        <h2>Related Artists</h2>
        <table>
            <thead>
                <tr>
                    <th>Artist</th>
                    <th>Relation</th>
                </tr>
            </thead>
            <tbody>
                {{range $artist, $relations := .Relations}}
                <tr>
                    <td>{{$artist}}</td>
                    <td>
                        {{range $index, $relation := $relations}}
                            {{if $index}}, {{end}}{{.}}
                        {{end}}
                    </td>
                </tr>
                {{end}}
            </tbody>
        </table>
    </section> -->
